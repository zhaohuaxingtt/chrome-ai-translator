# CONTEXT.md

> 本文件由 `/grill-with-docs` 访谈生成，记录项目的领域术语与关键决策。后续 `/to-spec`、`/to-tickets`、`/implement` 均以此作为共识基础。

## 项目简介

一个 Chrome 浏览器扩展（Manifest V3），调用外部 AI 模型实时翻译网页内容。默认双语对照展示，整个网页全部翻译，支持 IndexedDB 缓存以降低 API 调用成本。

## 关键决策

| # | 决策点 | 结论 |
| - | ------ | ---- |
| 1 | AI 接口 | 外部 API |
| 2 | 翻译范围 | 整个网页全部翻译 |
| 3 | 目标语言 | 用户手动选择 |
| 4 | 缓存 | IndexedDB |
| 5 | 翻译粒度 | 段落/文本块 |
| 6 | API 形态 | OpenAI 兼容接口（`baseURL + apiKey + model` 可配） |
| 7 | 执行方式 | 全量范围 + 可见优先 |
| 8 | 源语言 | 自动检测 |
| 9 | 译文展示 | 双语对照（原文保留变灰，译文并排正常展示） |
| 10 | 站点控制 | 默认全局翻译 + 可配置排除列表 |

## 术语表

- **段落块（text block）**：提交给 AI 翻译的最小单位，通常是 DOM 中一个块级元素（`<p>` / `<li>` / `<div>`）内聚合的文本。
- **双语对照**：译文展示方式，原文保留且变灰，译文在旁边正常展示。
- **可见优先**：翻译执行策略，范围是整页，但按视口可见性排序，先翻可见区域，后台补翻其余。
- **Content Script**：注入到网页的脚本，负责提取文本、渲染译文、监听 DOM 变化。
- **Background Service Worker**：MV3 后台脚本，负责翻译调度、缓存读写、调用 AI API（API key 只存在于此处，不进 content script）。
